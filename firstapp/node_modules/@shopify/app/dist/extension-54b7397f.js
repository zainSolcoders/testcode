import { r as runGoExtensionsCLI } from './cli-852b5950.js';
import { e as extensionConfig } from './configuration-a702715b.js';
import { yaml } from '@shopify/cli-kit';

async function buildExtension(options) {
  options.stdout.write(`Building extension...`);
  const stdin = yaml.encode(await extensionConfig(options));
  await runGoExtensionsCLI(["build", "-"], {
    cwd: options.app.directory,
    stdout: options.stdout,
    stderr: options.stderr,
    stdin
  });
}

export { buildExtension as b };
//# sourceMappingURL=extension-54b7397f.js.map
